// Get all elements with the class 'step_input-dropdown-wrapper'
const elements = document.querySelectorAll('.step_input-dropdown-wrapper');

// Iterate over each element and add the 'is-hidden' class
elements.forEach((element) => {
element.classList.add('is-hidden');
});
